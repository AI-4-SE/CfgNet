[defaults]
action_warnings=True
home=~/.ansible
fact_caching_timeout=86400
executable=/bin/sh
internal_poll_interval=0.001

[galaxy]
cache_dir={{ ANSIBLE_HOME ~ "/galaxy_cache" }}
collection_skeleton_ignore=^.git$, ^.*/.git_keep$
server=https://test.ansible.com

[jinja2]
dont_type_filters=string, ppretty, json

[runas_become_plugin]
password=test123
user=user
test=
