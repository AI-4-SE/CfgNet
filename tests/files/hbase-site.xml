<configuration>

    <!-- HBase root directory in HDFS -->
    <property>
        <name>hbase.rootdir</name>
        <value>hdfs://namenode.example.com:9000/hbase</value>
        <description>The directory shared by region servers and master where HBase stores data in HDFS.</description>
    </property>

    <!-- HBase Master Port -->
    <property>
        <name>hbase.master.port</name>
        <value>16000</value>
        <description>The port for the HBase master.</description>
    </property>

    <!-- Memory size for HBase RegionServer heap -->
    <property>
        <name>hbase.regionserver.global.memstore.upperLimit</name>
        <value>0.4</value>
        <description>Maximum heap size a RegionServer's MemStore can use (40% in this case).</description>
    </property>

    <!-- Time between client retries -->
    <property>
        <name>hbase.client.pause</name>
        <value>100</value>
        <description>Time between client retries, in milliseconds.</description>
    </property>

</configuration>
